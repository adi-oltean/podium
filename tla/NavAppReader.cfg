\* Experiment E-NAV (docs/add-tla-specs.md WP4): a second task reads the
\* module state without going through the pipe.  This run MUST FAIL —
\* TLC produces the interleaving that observes g between update and
\* predict.  The CI lane inverts the exit code and greps for the
\* NoPartialPublish violation (falsification receipt N4).
SPECIFICATION Spec
CONSTANT NMsgs = 4
CONSTANT QCap = 16
CONSTANT ReaderEnabled = TRUE
INVARIANT NoPartialPublish
